import React from "react";
import { GraduationCap, LayoutDashboard, MessageCircle, BookOpen, PencilLine, ClipboardList, TrendingUp, Trophy, Award, MessageSquareText, Settings, Users, BarChart3, ListChecks, Clock3, Database, KeyRound, CheckCircle2, LogOut } from "lucide-react";
import { logout } from "../../services/authService";
const icons={dashboard:LayoutDashboard,tutor:MessageCircle,comic:BookOpen,practice:PencilLine,exam:ClipboardList,progress:TrendingUp,rank:Trophy,badge:Award,reflection:MessageSquareText,profile:Settings,grades:ClipboardList,analytics:BarChart3,question:ListChecks,time:Clock3,knowledge:Database,access:KeyRound,attendance:CheckCircle2,users:Users};
export default function Sidebar({mode,screen,items,onNavigate,onLogout,session}){
 return <aside className="sidebar ac-sidebar"><div className="ac-brand"><div className="ac-brand-mark"><GraduationCap size={22}/></div><div><div className="ac-brand-name">AC-ITS</div><div className="ac-brand-sub">E-Comic Learning</div></div></div><div className="ac-profile"><div className="avatar">{session?.name?.[0]||"A"}</div><div className="profile-copy"><strong>{session?.name|| (mode==="teacher"?"Guru":"Ahmad")}</strong><span>{mode==="teacher"?"Guru":"Siswa"}</span></div></div><nav className="ac-nav">{items.map(([id,iconKey,label])=>{const I=icons[iconKey]||BookOpen;return <button key={id} className={screen===id?"ac-nav-item active":"ac-nav-item"} onClick={()=>onNavigate(id)}><I size={19}/><span>{label}</span></button>})}</nav><div className="ac-sidebar-footer"><button className="ac-mode-button logout" onClick={onLogout}><LogOut size={17}/><span>Keluar</span></button></div></aside>
}
